apiVersion: v1
kind: Service
metadata:
  name: aidconnect-frontend-service
  namespace: aidconnect-staging
  labels:
    app: aidconnect-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: aidconnect-frontend
